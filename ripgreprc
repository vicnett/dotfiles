# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=100
--max-columns-preview

# Search hidden files / directories (e.g. dotfiles) by default
--hidden

# Always match newlines with .
--multiline-dotall

# Search case-insensitively if the pattern is all-lowercase, and case-sensitively
# otherwise.
--smart-case
