- defaults:
    link:
      relink: true

- create:
  - ~/.config/

- link:
    ~/.zshrc:
      force: true
      path: zsh/zshrc
    ~/.gitignore: git/gitignore
    ~/.config/nvim: vim
    ~/.byobu: byobu

- shell:
  - [git submodule update --init --recursive, Installing submodules]
