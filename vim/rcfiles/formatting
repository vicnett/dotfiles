" Formatting settings
"--------------------

set textwidth=80

" only enforce line length in comments, plus other options (see :h fo-table)
set formatoptions=jcroql1

" Softtabs, 2 spaces
set tabstop=2
set shiftwidth=2
set shiftround
set expandtab

" Use one space, not two, after punctuation.
set nojoinspaces

" vim:ft=vim
